# Price Alert Frontend

React and Vite client for creating, viewing, and deleting price alerts.

## Setup

```powershell
npm install
Copy-Item .env.example .env
npm run dev
```

Set `VITE_API_URL` if the API is not running at `http://localhost:8000`.
